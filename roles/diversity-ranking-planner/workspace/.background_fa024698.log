# Top-100 去重、多样性与排序设计 — 会议共享副本

本文档是专门针对【最终 top-100 去重、多样性与排序】的深入设计，供会议其他参与者审阅。

## 核心设计摘要

### 1. 不依赖纯分数排序
采用 **圆桌轮转（Round-Robin with Priority）** 机制，综合分高的图像不会垄断名额。每轮从不同簇中按 `priority_score = composite_score - decay_factor * selected_count[cluster]` 选择，确保分数高+簇不饱和的优先，但簇饱和后自动跳过。

### 2. 去重：贪心密度球
特征空间 cosine distance，按综合分降序贪心保留，移除距离 < threshold 的近似品。阈值自适应：取第 5 百分位 pairwise distance × 1.5，范围 [0.08, 0.25]。房产类收紧到 0.10，truro_school 放松到 0.18。

### 3. 多样性：三层次配额
| 层级 | 上限 |
|------|------|
| 粗场景簇 | ≤ 30 张 (30%) |
| 细视点簇 | ≤ 15 张 (15%) |
| 单人近景 | ≤ 25 张 (25%) |
| 最低代表 | ≥ 3 个场景簇 |

### 4. 高相似集合自适应
根据聚类结果自动判断：若最大簇占比 > 35% 且簇数 < 4 且平均 pairwise distance < 0.25，自动启用更严配额（场景簇 ≤ 20，视点簇 ≤ 8）+ KL 散度偏置检测自动下调过集中簇的配额。

### 5. truro_school 特殊策略
- 去重阈值 0.18（防过度去重导致数量不足）
- 配额：场景簇 ≤ 15 张，视点簇 ≤ 5 张
- 每簇选 top-2 + 随机 1 张（多样性注入）
- 若去重后候选 < 100，降级到 0.22 重跑

### 6. 复核池
被去重的高分样本（≥ 0.70）、因配额未入选的 top-150、DBSCAN 噪声点、高质量人像（≥ 0.85）均进入复核池供人工审查替换。

### 7. 综合分公式
```
composite_score = 0.5 × realism_confidence + 0.3 × quality_score + 0.2 × display_fit_score
```
可配置权重。

### 8. 单人近景检测（纯视觉）
宽高比 + 最短边 + edge_ratio + colorfulness + face_ratio（OpenCV Haar Cascade，CPU 5ms/张），不依赖文件名。

---

完整设计文件见：`workspace/dedup_diversity_ranking_design.md`
